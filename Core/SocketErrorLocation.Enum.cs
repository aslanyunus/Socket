namespace NetSocket.Core
{
    public enum SocketErrorLocation
    {
        BindAndListen,
        BeginStart,
        EndStart,
        Receive,
        Shutdown,
        Close,
        DisconnectAndDispose
    }
}
